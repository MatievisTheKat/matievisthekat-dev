"use strict";
const head = document.querySelector('img#head');
const torso = document.querySelector('img#torso');
const legs = document.querySelector('img#legs');
const feet = document.querySelector('img#feet');
if (!head)
    console.warn('[buildAMatt] img#head not found');
if (!torso)
    console.warn('[buildAMatt] img#torso not found');
if (!legs)
    console.warn('[buildAMatt] img#legs not found');
if (!feet)
    console.warn('[buildAMatt] img#feet not found');
if (head && torso && legs && feet) {
}
//# sourceMappingURL=buildAMatt.js.map