{
  "license": "MIT",
  "scripts": {
    "postcss": "postcss css/raw/**/*.css --base css/raw --dir css/prod --config postcss.config.js --map",
    "sass": "sass sass:css/raw --no-source-map",
    "tsc": "tsc",
    "watch": "yarn sass --watch & yarn postcss --watch & tsc -w"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "cssnano": "^5.0.14",
    "postcss": "^8.4.4",
    "postcss-cli": "^9.0.2",
    "postcss-preset-env": "^7.0.1",
    "postcss-reporter": "^7.0.4",
    "sass": "^1.44.0",
    "stylelint": "^15.10.1",
    "typescript": "^4.5.2"
  }
}
